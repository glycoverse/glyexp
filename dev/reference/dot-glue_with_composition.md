# Format variable strings with special handling for glycan_composition

Format variable strings with special handling for glycan_composition

## Usage

``` r
.glue_with_composition(var_info, format)
```
