% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/glyco-se.R
\name{GlycomicSE}
\alias{GlycomicSE}
\title{Create a GlycomicSE object}
\usage{
GlycomicSE(abundance, ...)
}
\arguments{
\item{abundance}{A numeric abundance matrix with glycans as rows and samples
as columns.}

\item{...}{Arguments passed to \code{\link[SummarizedExperiment:SummarizedExperiment-class]{SummarizedExperiment::SummarizedExperiment()}}.
\itemize{
\item \code{rowData}: A \code{S4Vectors::DataFrame()} with at least the following columns:
\itemize{
\item \code{glycan_composition}: required, a \code{glyrepr::glycan_composition()} vector
\item \code{glycan_structure}: optional, a \code{glyrepr::glycan_structure()} vector
}
\item \code{colData}: A \code{S4Vectors::DataFrame()}.
\item \code{metadata}: A list. It must include a \code{glycan_type} field with one of
\code{"N"}, \code{"O"}, \code{"O-GalNAc"}, \code{"O-GlcNAc"}, \code{"O-Man"}, \code{"O-Fuc"}, or
\code{"O-Glc"}.
}}
}
\value{
A \code{GlycomicSE} object.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

\code{GlycomicSE()} creates a single-assay
\code{\link[SummarizedExperiment:SummarizedExperiment-class]{SummarizedExperiment::SummarizedExperiment()}} subclass for glycomics data.
It is a thin wrapper around \code{SummarizedExperiment()} with additional
Glycoverse validation:
\enumerate{
\item Exactly one assay is allowed. Extra assays are rejected to avoid
ambiguous glycomics measurements.
\item The assay must contain only non-negative values. Raw glycomics abundance
data are non-negative; log transformation should be handled by downstream
Glycoverse packages.
\item \code{rowData} must contain a \code{glycan_composition} column, and that column must
be a \code{glyrepr::glycan_composition()} vector. A \code{glycan_structure} column is
optional, but if present it must be a \code{glyrepr::glycan_structure()} vector.
\item \code{metadata} must contain a \code{glycan_type} field.
}

This container is experimental and is not recognized by all Glycoverse
packages yet. It is intended to become a recommended entry point as package
contracts migrate toward \code{SummarizedExperiment}-based containers.
}
\seealso{
\code{\link[=GlycoproteomicSE]{GlycoproteomicSE()}}
}
