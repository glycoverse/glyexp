% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/se.R
\name{from_se}
\alias{from_se}
\title{Convert SummarizedExperiment to experiment}
\usage{
from_se(se, assay_name = NULL, exp_type = NULL, glycan_type = NULL)
}
\arguments{
\item{se}{A \code{SummarizedExperiment} object to convert.}

\item{assay_name}{Character string specifying which assay to use.
If NULL (default), uses the first assay.}

\item{exp_type}{Character string specifying experiment type.
Must be either "glycomics", "glycoproteomics", or "others".
If NULL, will try to extract from metadata, otherwise defaults to "glycomics".}

\item{glycan_type}{Character string specifying glycan type.
Must be either "N" or "O".
If NULL, will try to extract from metadata, otherwise defaults to "N".}
}
\value{
An \code{\link[=experiment]{experiment()}} object.
}
\description{
Convert a \code{SummarizedExperiment} object to an \code{\link[=experiment]{experiment()}} object.
This function maps the SummarizedExperiment structure to experiment format:
\itemize{
\item The main assay becomes \code{expr_mat}
\item \code{colData} becomes \code{sample_info}
\item \code{rowData} becomes \code{var_info}
\item \code{metadata} becomes \code{meta_data}
}
}
\examples{
# Convert SummarizedExperiment back to experiment
se <- as_se(toy_experiment)
exp_back <- from_se(se, exp_type = "glycomics", glycan_type = "N")
exp_back

}
