# Show a GlycoproteomicSE object

Show a GlycoproteomicSE object

## Usage

``` r
# S4 method for class 'GlycoproteomicSE'
show(object)
```

## Arguments

- object:

  A `GlycoproteomicSE` object.

## Value

Invisibly returns `object`.
