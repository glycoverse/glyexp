# getting glycan structures without add_structures raises error

    Code
      get_glycan_structures(exp)
    Condition
      Error in `get_glycan_structures()`:
      ! Glycan structures are not parsed. Please call `add_structures()` first.

