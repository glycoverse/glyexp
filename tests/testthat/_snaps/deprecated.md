# deprecated dplyr aliases warn and preserve behavior

    Code
      old <- filter_obs(real_experiment, group == "H")
    Condition
      Warning:
      `filter_obs()` was deprecated in glyexp 0.16.0.
      i Please use `filter_col()` instead.

